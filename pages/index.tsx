import { Box } from "@mui/material";
import Index from "@src/pages/index";

const Home = () => {
  return (
    <Box sx={{minHeight: "100%"}}>
      <Index />
    </Box>
  );
};

export default Home;
